# F4 首页架构计划 · 历史实施顺序归档

> **AI 默认不读取。** 本文件仅供追溯 2026-07-17 至 2026-07-19 的历史实施顺序；只有明确调查旧阶段决策时才按需读取。当前架构以 `../F4_HOME_ARCHITECTURE_PLAN.md` 的活跃 §0–§9、未决 §11–§12 和有效 §13 为准。

归档来源：`F4_HOME_ARCHITECTURE_PLAN.md` 原 §10，归档日期 2026-07-22。以下内容保持历史语义，不代表当前待办。

## 10. F4 正式接入分阶段实施顺序（历史）

每阶段完成即按契约 §9 流程走一遍(状态接口 → 视觉 → 自动检查 → 真机 → 确认 → 锁定)。

**2026-07-19 后续实施顺序（覆盖下方旧阶段描述中与长期农场冲突的部分）:**

1. **纯规则:**先实现奖励、单巢、稀有度与保底、章节资格、40/8 可见集合、价格和三档料理纯函数；不接 UI。
2. **数据库迁移:**增加 Dexie v3 与 v1/v2/v3 备份兼容，用真实旧数据夹具验证鸡、蛋、完成日零损失。
3. **当前农场闭环:**单巢、破壳、稀有鸡、鸡舍/最喜欢、厨房及旅行退款先在第 1 场景走通。
4. **章节与收藏系统:**地图、36 日庆祝和顺序旅行、贴纸购买/摆放、衣柜/装备、合照与内容脱期兜底。
5. **美术门槛:**批量生产前先通过单巢、分层小皮/母鸡、三类小鸡和三层贴纸技术原型；核心包始终预置后续两章。

下列阶段 A–H 是 2026-07-17 F4 首次接入历史，已完成状态不代表上述 1–5 已完成：

- **阶段 A · 纯架构重构(V-8 裁决后修订)**
  目录重构(§1)、Dexie v2 + 迁移 + 测试、蛋经济/陈旧度/翻转用例落地;
  **删除全部旧视觉组件**(SVG 角色、旧屏 UI、旧换肤样式),回归基准改为
  单元/集成测试 + 带 `DEV PLACEHOLDER` 标记的内部开发壳(仅驱动逻辑验证)。
  DoD:全测试绿 + 开发壳可走通"完成学习→发蛋→分配→24h 孵化"全链路。
- **阶段 B · 固定舞台外壳**
  `useStageScale`/`toStagePoint`/横竖屏与 Split View 门槛逻辑(Claude);
  `.f4-viewport/.f4-bleed/.f4-stage` CSS 与竖屏引导视觉(Codex);
  资产复制 `design-samples/assets/* → public/assets/f4/`(母版不动,登记副本哈希)。
  DoD:GM-01/IP-11 视口下舞台等比 + 背景延展正确。
  (**2026-07-17 爸爸裁决:当前仅适配 iPad Pro 11" A2759 横屏**;IP-13、竖屏、
  Split View 等其他尺寸适配推迟至完整版正式上线后,回归矩阵相应项暂缓。)
- **阶段 C · daily_incomplete 单状态接真数据**
  FarmHome 状态机 + VM + 事件桥(Claude);Codex 把 F4 母版 HTML/CSS/JS 移植为
  React 视觉层(散步/互动/拖拽脚本平移进 `visual/`),顶栏与任务板绑 VM。
  文案模板差异先走 §11 V-2 确认。DoD:真实数据渲染 + 黄金截图差异仅限已批准文案。
  **状态:✅ 2026-07-17 完成。**`daily_incomplete` 已进入生产 React 视觉层;F4 母版未改;
  31/31 测试、生产 PWA 构建与 1194×834 / 1366×1024 / 竖屏浏览器验收通过。
- **阶段 D · 蛋经济闭环**
  完成学习 → 发蛋 → 面板分配 → 孵化/煎蛋/喂食 → 次日翻转孵出小鸡 → 农场 +1;
  救援入口计数真实化(救援屏本体属后续屏)。DoD:两日连续真机走查全链路。
  **状态:✅ 2026-07-17 完成。**真实 Dexie 两日闭环、无小鸡喂食保护、到达小鸡后提交喂食、
  并发时钟守卫均已验证;33/33 测试、生产 PWA 构建与 1194×834 浏览器走查通过。
  学习页与 `daily_complete` 正式视觉仍属后续屏幕阶段,本阶段使用仅开发环境可见的真实数据库走查入口。
- **阶段 E · 群聊 + 拖拽持久化**
  CHICK_CHAT(陈旧度抽词 + TTS + seen 更新;多气泡行为先过 §11 V-4)、
  CHICK_PLACED 持久化、40/鸡舍收纳数据侧。
  **状态:✅ 2026-07-17 实现与浏览器验收完成。**真实小鸡/有效词库守卫、原子 `seen` 更新、
  4 秒气泡、快速点击竞态保护、TTS 无声降级、`pointercancel` 回滚、落点刷新恢复与 40/鸡舍数据侧通过;
  39/39 测试及生产 PWA 构建通过。A2759 实际发声抽听保留为完整上线前真机门槛。
  V-4 未裁决前正式页保持“单气泡 + 真实抽词”;V-5 只完成数据侧,不启用 40 只视觉密度。
- **阶段 F · 三状态补全**
  first_visit(场景名牌起名)与 daily_complete(连胜展示牌 + 游戏入口木牌):
  依赖 P0 资产与新视觉状态确认(§11 V-3),按变更单流程推进。
  **状态:✅ 2026-07-17 工程实现与浏览器验收完成。**首次起名名牌、完成态连胜牌、
  写词游戏入口及 `OPEN_HANDWRITING_GAME` 导航意图已接入生产 React 视觉层；GitHub Pages
  JSX 图片地址统一改用 `BASE_URL`，本次基线 42/42 测试、Pages 子路径生产构建与 1194×834 浏览器走查通过。
  V-3 正式 PNG 资产和小皮视觉锁定仍按变更单保留，不阻塞当前 CSS 场景物件上线预览。
- **阶段 G · PWA 收口与部署(2026-07-17 爸爸定:GitHub Pages)**
  manifest(standalone + landscape)、图标从 `pwa-icon-farm-master.png` 无损派生
  (180/192/512/maskable,禁止 AI 重画)、SW 预缓存清单、OFF/RM 回归、真机验收;
  Vite `base` 子路径 + PWA scope 适配 GitHub Pages;
  **备份裁决(2026-07-17 爸爸最终定):**不做 GitHub 云备份，不配置 PAT，公开
  `backups` 分支保持不用；家长页只提供手动 JSON 导出/导入和定期导出提醒。
  部署管道建议在阶段 B/C 期间先行搭好,服务每屏真机验收。

  **部署基线已提前完成(2026-07-17 Claude):**公开仓库 `bebop-m/pipienglish`、GitHub Pages
  `https://bebop-m.github.io/pipienglish/`、`main` 推送触发测试/构建/发布的 Actions 工作流与
  `backups` 分支均已就位。Codex 同日只读复核:仓库公开、最近工作流成功,首页/JS/CSS/
  manifest/Apple 图标子路径请求均为 HTTP 200。

  **状态:🟡 2026-07-17 PWA 核心收口完成，等待 A2759 真机验收。**manifest 已固定
  standalone + landscape 与 Pages 相对 scope；生产图标已从锁定母图机械派生并登记哈希；
  全部 F4 PNG 已进入 Workbox 预缓存；iPad Safari 网页态安装提示、RM 浏览器回归与生产构建通过。
  OFF 冷启动、主屏幕图标、安全区、触控和 TTS 保留为 A2759 standalone 真机门槛。
  经 GitHub 官方文档复核，公开仓库的任一分支都不能作为私密学习数据存储；爸爸最终决定不建设
  独立私有仓库、不实现云备份，现有公开 `backups` 分支保持不用。本地 JSON 导出/导入与家长提醒
  作为唯一备份渠道。详见 `F4-CHG-006`。
