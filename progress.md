Original prompt: 批准按建议参数进行代码接入，暂不发布场景二、不提交、不推送。

# 当前有效状态

历史阶段记录已移至 [`docs/03-workflow/archive/progress-2026-07-17.md`](docs/03-workflow/archive/progress-2026-07-17.md)。该归档 **AI 默认不读取**，仅追溯旧阶段时按需打开。

## 2026-07-19 · 长期农场场景 1 发布收口

- 长期经济规格已落地：必修 2 蛋、写词游戏每日前 10 轮奖励、单巢 24 小时、83/15/2 稀有度与 10/36 次保底、40 只可见与每场景 8 个最喜欢、1/5/20 三档料理。
- Dexie v3、v1/v2/v3 备份迁移、鸡舍/收藏、料理退款旅行、36 完成日章节状态机和场景回访底层均已完成。
- 发布内容包暂时只包含 `scene-1`；`scene-2` 仍是未来草案，不进入 `availableChapter`，不出现在儿童地图或旅行入口。
- 场景 1 贴纸与衣柜底层已完成；正式图片未获小皮批准，生产目录继续过滤 `internal-placeholder`，儿童界面不显示商品入口或锁定剪影。
- 框架 8 阶段 A 候选图未接入；首页继续使用已批准的场景 1 背景与标准小皮/母鸡造型。
- 最近发布验证基线：40 个测试文件、214/214 通过，生产 PWA 构建成功；场景 1 可见、无下一章、商品目录为 0。

## 当前待办

- 重做并审批框架 8 单张视觉校准；批准前不批量生产、不接入候选 PNG。
- 校准获爸爸/小皮批准后，冻结分层交付清单，再制作场景 2 核心包与场景 1 正式贴纸/装扮。
- 完成阶段 B 真机视觉矩阵与 PWA 断网门槛。
- “当前章＋下一章”五处产品规格变更未获本任务授权；如需实施，必须由爸爸另签并建立独立 F4-CHG。

## 2026-07-22 · 开发减负方案（文档与流程）

- 新增任务门禁、按任务路由、执行预算和 L1/L2/L3 验证分级，减少重复上下文与无目的图片读取。
- 历史进度和首页架构原 §10 已归档；`docs/03-workflow/changes/` 保持原位、未移动、未压缩。
- 本轮不修改运行时代码、儿童可见行为、产品规格或生产资产；不 push、不部署、不清理 worktree。

## 2026-07-22 · 场景一／二冻结孵化素材代码接入

- G4 仅资产复制完成后，已开始按爸爸批准的参数接入：完整蛋 `>50%`、细裂纹 `20%–50%`、大裂纹 `≤20%`；两半蛋壳 `800ms`、结果贴图 `1500ms`。
- 场景一／二均登记八张完整鸡窝状态；运行时不再把独立鸡蛋、裂纹或蛋壳叠到空窝上。
- 普通小鸡继续使用 `chick-f3.png`；异色只映射批准 B；特殊只映射批准 F，并采用 G3 批准的放大画布与两场景 anchor。
- 场景二仍只存在于 `FUTURE_FARM_SCENE_DRAFTS`，未加入生产发布列表；不提交、不推送。
- 验证完成：Playwright 已检查空窝、细裂纹、大裂纹、两半蛋壳、特殊出壳和特殊 F 入场，截图与 `render_game_to_text` 一致且控制台 0 错误；完整测试 45 个文件、250/250 通过；生产 PWA 构建成功。
- 场景二资产继续不进入当前 Service Worker 预缓存；场景一八状态与共享异色 B／特殊 F 已进入离线内容包。后续仍需爸爸单独批准场景二发布、提交和推送。
- 按爸爸反馈把鸡窝下方的大说明框收成简约倒计时胶囊：仅显示“等待鸡蛋／10 小时／25 分钟／破壳中／新朋友”，完整说明改由无障碍标签提供；Playwright 截图确认尺寸与鸡窝协调，控制台 0 错误。
- 爸爸不喜欢胶囊样式后，已基于真实场景一与冻结鸡窝制作三张只改 CSS 的局部候选：A 手作纸签、B 迷你木牌、C 无框浮字。样板保存在仓库外 `countdown-style-samples/`；本轮未改生产代码或冻结贴图。三张均在细裂纹状态显示“10 小时”，`render_game_to_text` 状态一致，控制台 0 错误；另用官方 web-game Playwright 客户端完成一次页面基线检查。
- 为解决未来场景主题适配问题，进一步制作 9 个系统级中性倒计时候选：环形进度 1A/1B/1C、细进度线 2A/2B/2C、全局状态徽标 3A/3B/3C，并输出三张横向对比板至仓库外 `countdown-universal-9/`。全部只做运行时 CSS 样板，冻结鸡窝和生产样式未改；真实细裂纹状态与“10 小时”一致，控制台 0 错误，官方 web-game Playwright 基线检查完成。
- 爸爸批准先采用 2A“上字下线”：正式样式已改为无框剩余时间＋短进度线，橙色段按 24 小时孵化周期实时表示已完成比例；空窝为 0%，破壳过渡为 100%。新增进度钳制单测，定向测试 6/6 与 TypeScript 检查通过；Playwright 实测空窝和细裂纹 10 小时状态，60% 进度与 `render_game_to_text` 一致且控制台 0 错误。冻结贴图、场景二发布、提交和推送均未触碰。
- 最终验收包已完成：空窝、10 小时、25 分钟、破壳中、新朋友五态视觉与状态数据一致；1024×768 横屏正常，竖屏／过矮横屏按既有规则显示旋转或全屏提示。完整测试 45 文件、251/251 通过，生产 PWA 构建及全屏视觉层守卫通过；33 个清单资产 SHA-256 全匹配，场景一八张孵化状态进入预缓存而场景二仍为 0，控制台 0 错误。报告和截图位于仓库外 `final-hatchery-acceptance/`；未发布场景二、未提交、未推送。

## 2026-07-22 · 场景二正式发布与分支同步

- 爸爸批准“剩下的全部合并执行推送同步”；按安全边界解释为完成场景二正式发布、合并剩余工作树文档、提交推送当前分支并创建／同步 PR，不直接合并 `main`、不部署。
- 场景二已从未来草案进入 `FARM_SCENE_DEFINITIONS`，生产 `availableChapter=2`；第 36 个完成日后按庆祝与顺序旅行进入苹果园。未批准路牌、贴纸和装扮继续由 `assetStatus` 过滤。
- PWA 已纳入场景二 12 个生产资产，背景单文件缓存上限提高到 6 MB；生产构建预缓存 44 项约 35.5 MB，场景一／二路径引用分别为 8／12。
- Playwright 已验收苹果园完整页及空窝、10 小时、25 分钟、破壳中、新朋友五态；页面没有 `internal-placeholder` 图片节点，目录项为 0，控制台与资源请求错误均为 0。
- 完整测试 45 文件、251/251 通过，TypeScript、全屏视觉层守卫和生产 PWA 构建通过；验收材料位于仓库外 `scene2-release-acceptance/`。
