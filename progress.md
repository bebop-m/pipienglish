Original prompt: 继续（承接阶段 F 已上线，进入阶段 G）

## 2026-07-17 · 阶段 G 开始

- 当前线上基线：`1e100ee`，阶段 E/F 与 GitHub Pages PNG 子路径修复已部署成功。
- 当前适配范围：只锁定 iPad Pro 11 英寸 A2759 横屏；其他尺寸留到完整版上线后。
- 共享工作区中 `src/domain/words.ts` 的删除及 `src/domain/words/`、`src/domain/lesson*.ts` 是 Claude 正在整理的未提交工作；Codex 不覆盖、不混入本轮提交。
- 本轮目标：审计并收口阶段 G 中无需外部密钥的 PWA 项（manifest、图标、SW 离线、安装提示、RM/OFF 回归）；GitHub 云备份需单独核对安全边界。

## TODO

- [x] 核对 PWA 图标是否确由锁定母图机械派生并登记哈希。
- [x] 将 manifest 方向从 `any` 收口到当前裁决的 `landscape`，核对 Pages scope/start_url。
- [x] 检查并补齐离线预缓存；断网冷启动保留为 A2759 真机门槛。
- [x] 增加只在 iPad Safari、未安装状态出现的“添加到主屏幕”提示条。
- [x] 明确公开仓库隐私风险；禁止向现有公开 `backups` 分支写明文学习数据。
- [x] 爸爸最终决定不做云备份；只保留本地数据与家长页手动 JSON 导出/导入。

## 本轮进展

- 已从锁定母图纯缩放派生 180/192/512 图标，并新增带安全边距的 512 maskable 图标；未使用生成式重画。
- manifest 已补 `id`、`start_url`、`scope`、`zh-CN` 与 `landscape`，Pages 子路径保持相对解析。
- Workbox 预缓存从 8 项扩展为 20 个唯一条目（约 9.7 MiB），九张 F4 PNG、图标、代码、CSS、manifest 均已进入离线清单。
- 新增只在未安装的 iPad Safari 显示的“添加到主屏幕”提示及四项纯函数测试。
- 技能提供的独立 Playwright 客户端已尝试运行；本机项目未安装 Playwright，Codex 工作区包又缺少其 `playwright-core` 同级依赖，因此未给项目新增约 100+ MiB 的浏览器依赖。后续视觉/离线回归继续使用已配置的应用浏览器与浏览器原生 Service Worker 检查。
- 本地生产预览在线首开与二次加载时，F4 图片均完成解码；浏览器安全策略禁止在本地服务器关闭后再次导航，因此“断网刷新”不能在该自动化表面完成，保留为 A2759 真机验收。静态 SW 清单已经逐项确认包含全部 F4 PNG。
- GitHub 官方资料确认公开仓库对互联网所有人可见，PAT 应按密码保护；现有公开仓库的 `backups` 分支不具备隐私边界，云备份实现等待安全存储方案裁决。
- 爸爸最终裁决：这是小皮个人使用的软件，不建设私有备份仓库；公开 `backups` 分支保持不用。
- A2759 主屏幕截图发现首版图标存在双重白边；已定位为锁定母图自带的 30px 展示画布，并以机械裁边后重新派生解决，未重画小皮选择的图案。

## 2026-07-17 · 阶段 H 前置修正与审批规则

- 爸爸反馈首页品牌仍使用 emoji、缺少“の”，字体在 A2759 上过细过锐；品牌改用真实 `chick-f3.png`，正式名称统一为“皮皮のEnglish”，字体改为系统圆体优先并提高正文基础字重。
- A2759 截图中的左右背景拼接缝来自 `.f4-bleed` 与 `.f4-stage` 分别 cover 同一背景；改为整屏背景只绘制一次，舞台透明叠加。
- 阶段 H 起每个学习状态都先产 1194×834 视觉候选截图，交小皮确认；批准前不接生产导航、真实数据或锁定基准。
- H-1A 有图听看卡已迭代到 V5：删除卡片顶部黄色图钉，例句右侧只保留无圆框、无底色的音频波形图标；截图为 `visual-regression/stage-h-candidates/H1-intro-egg-v5-1194x834.png`；随后已获小皮批准并接入生产组件。
- 单词图片改为可选：按一年最多约 800 词规划，其中约 400–500 个高频具象词制作专属图，目标覆盖率约 60%；抽象词使用 H-1B 文字优先候选，缺图时不使用 emoji、破图或空图片框。详见 `docs/03-workflow/F4_WORD_ILLUSTRATION_POLICY.md`。
- H-1B 无图听看卡已迭代到 V4：删除卡片顶部黄色图钉，例句右侧只保留无圆框、无底色的音频波形图标；截图为 `visual-regression/stage-h-candidates/H1B-intro-because-v4-1194x834.png`；随后已获小皮批准并接入生产组件。
- H-1 正式实现约束：点击例句调用现有 `speak(sentence)`；重复点击先取消上一段，不新增音频文件或服务端请求。候选仅展示交互入口，小皮批准 H-1A/H-1B 后再接生产行为。

## 2026-07-17 · H-1 小皮批准与生产接入

- 小皮确认 H-1A 有图听看卡 V5 与 H-1B 无图听看卡 V4 均没有问题；两项状态已改为 `xiaopi-approved`。
- 已实现统一生产 `LessonIntroScreen`：有批准插图时使用有图版，缺失或未知 `imageAssetId` 时使用无图版，不回退 emoji、破图或空图片框。
- 初次进入自动朗读单词两遍，500ms 后显示词形；“再听一次”和整张例句卡分别播放单词与英文例句。
- H-1A/H-1B 已在 1194×834 下完成视觉与点击回归，生产截图位于 `visual-regression/stage-h-production/`，控制台无错误。
- 正式首页入口继续门控：等 H-2 及后续学习状态逐屏获得小皮批准后再接完整导航，避免孩子进入半成品学习流。下一项为 H-2 描红卡视觉候选。

## 2026-07-17 · H-2 描红卡视觉候选

- H-2 拆为两个必须分别确认的孩子可见状态：H-2A 准备描红、H-2B 描红完成；均保持 1194×834 与 H-1 同一顶部进度语法。
- H-2A V1 使用四线三格、超大淡灰 `egg` 字模、粉色蜡笔笔迹、重听/清除/写好了，并明确“只写一遍”“这里不打分”。
- H-2B V1 在完成后淡出字模，只保留示意字迹和“这是你的字”提示，不显示 OCR、对错或分数。
- 交互候选已验证 Pointer Events 跟手、清除、重听和完成；测试发现并修复“没有下笔也能完成”，现至少需要一笔才进入完成态。
- 技能自带 Playwright 客户端再次因项目未安装 `playwright` 无法运行；应用内浏览器完成落笔、清除、空提交保护、完成态及控制台回归，未新增大型依赖。
- 候选截图：`visual-regression/stage-h-candidates/H2A-trace-ready-v1-1194x834.png` 与 `H2B-trace-complete-v1-1194x834.png`；当前 `pending-xiaopi`，不接 `src/`。

## 2026-07-17 · 英文文本答案裁决

- 爸爸确认描红只练笔划、不判分；H-2 Canvas 与双状态候选保持不变。
- 默写、手写复习、手写单词游戏和救援补写统一使用标准英文文本输入框；小皮可按键输入，也可用 Apple Scribble 或第三方手写输入法，应用不区分输入来源。
- 判定对象是输入法转换后的英文文本，不是小皮笔划；因此取消这些场景的自建 OCR、笔迹识别、对照自评和笔迹保存要求，改为标准答案客观判断。
- 输入框关闭自动纠错/完成/拼写检查/自动大写；答案比较只归一首尾空白、Unicode 和英文大小写，内部空格、撇号与连字符仍按词库答案判断。
- 详细裁决见 `docs/03-workflow/changes/F4-CHG-012-text-answer-input-decision.md`；H-5 视觉候选按新规则设计。

## 2026-07-17 · H-2 小皮批准与生产接入

- 小皮确认 H-2A 准备描红与 H-2B 描红完成 V1 均没有问题，两项状态已改为 `xiaopi-approved`。
- 已实现正式 `LessonTraceScreen`：四线三格字模、连续书写、清除、重听、空画布保护和完成态淡入均按批准稿落地。
- 输入设备统一按普通电容触控处理：电容笔、手指、鼠标都走标准 Pointer Events；不识别笔型、不读取压感，也不为 Apple Pencil 单独适配或优化。
- 描红继续严格不判分，不做 OCR、笔划顺序或相似度判断；只过滤单点误触，保留孩子自己的笔迹。
- 1194×834 浏览器回归已覆盖初始态、书写、清除、完成反馈与控制台；生产截图位于 `visual-regression/stage-h-production/`。
- 自动测试 74/74 通过且生产构建成功；技能独立 Playwright 客户端仍因环境未安装 `playwright` 无法启动，未为此给项目增加大型浏览器依赖。
- 首页学习入口继续门控；下一项为 H-3 选择题视觉候选，仍须先给小皮看图再接生产。

## 2026-07-17 · H-3 选择题视觉候选

- H-3 拆为 H-3A 准备、H-3B 答对、H-3C 答错重试三个孩子可见状态，均已输出 1194×834 候选图。
- 爸爸在首轮预览中发现选项下显示英文会直接泄题；该版立即作废，正式 V1 候选的四个选项只保留中文，英文只出现在顶部题目单词。
- 答对使用低饱和绿色确认并显示“继续”；答错使用低饱和粉色，只标记本次错选并保留重听、重选机会。
- 当前三张图均为 `pending-xiaopi`，只位于 `design-samples/` 与候选截图目录，不接 `src/`、生产导航或真实判分。

## 2026-07-17 · H-3 小皮批准与生产接入

- 小皮验收 H-3A 准备、H-3B 答对、H-3C 答错重试 V1 均没有问题，三项已改为 `xiaopi-approved`。
- 已实现正式 `LessonChoiceScreen`：选项只渲染中文 label，使用稳定 id 客观判定，不会泄露对应英文。
- 答错后保留当前题、重听与重新选择；答对后锁定选项并保留绿色确认画面，点击“继续”才离开。
- 每次作答通过事件桥交给上层；队尾重考和持久化仍由 application 层负责，视觉组件不越层写数据库。
- 自动测试 82/82 与生产构建通过；1194×834 浏览器完整回归和三张生产截图通过，控制台无错误。
- 技能独立 Playwright 客户端仍因环境缺少 `playwright` 无法启动；继续使用已配置的应用浏览器完成交互回归，未向项目引入大型浏览器依赖。
- 首页学习入口继续门控；下一项为 H-4 听音题视觉候选，仍须先经小皮审批。

## 2026-07-17 · H-4 听音题视觉候选

- H-4 拆为 H-4A 准备、H-4B 答对、H-4C 答错重试三个孩子可见状态，均已输出 1194×834 候选图。
- 准备态和重试态严格隐藏目标英文与音标，只显示大音频入口和四个纯中文选项；答对后才揭示 `egg /eɡ/` 做听形绑定。
- 答错只标记本次错选，保留重听与重选；答对显示绿色确认和继续按钮，整体反馈语法与已批准 H-3 一致。
- 当前三张图均为 `pending-xiaopi`，只归档候选素材，不接 `src/`、生产 TTS、真实判分或孩子可见导航。

## 2026-07-17 · H-4 小皮批准与生产接入

- 小皮确认 H-4A 准备、H-4B 答对、H-4C 答错重试 V1 均没有问题，三项已改为 `xiaopi-approved`。
- 已实现正式 `LessonListeningScreen`：进入题目自动播放一次，音频入口可重复播放，选项只显示中文。
- 准备态和重试态完全不渲染目标英文与音标；浏览器页面文本检查均未发现 `egg` 或 `/eɡ/`，答对后两者才出现。
- 答错保留本题和重听重选；答对锁定选项并保留绿色确认，点击继续后才离开。
- 自动测试 97/97、生产构建和 1194×834 新增状态回归通过，控制台无错误；没有重复手工回归 H-1 至 H-3。
- 后续测试策略收紧为“新增屏逐状态检查、提交前完整自动测试、阶段 H 完成后全流程回归、里程碑再做 iPad 真机回归”，减少重复截图与额度消耗。
- 下一项为 H-5 默写/收尾视觉候选，仍须小皮先看图审批。

## 2026-07-17 · H-5 默写题视觉候选

- H-5 拆为 H-5A 准备、H-5B 答对、H-5C 答错重试、H-5D 看答案四个孩子可见状态，均已输出 1194×834 候选图。
- 输入区使用标准英文文本框，键盘、普通电容笔或系统手写输入最终都进入同一字符串判断；不做 OCR、笔迹或设备识别。
- 准备态不显示目标英文；答错保留本次输入供修改；看答案显示 `egg /eɡ/` 但不自动填入，要求回到输入框自行再写。
- 当前四张图均为 `pending-xiaopi`，只归档候选素材，不接生产判定、课程状态或孩子可见导航。

## 2026-07-17 · H-5 V2 默写与小鸡救援规则

- H-5 V1 的“看一眼答案/看答案态”整组作废；默写准备态和答错重试态统一只显示“想不起来”，点击后不揭示答案并直接跳过本题。
- 跳过的单词进入错题/救援队列，同时一只小鸡被抓；新增 H-5D 被抓结果画面，明确孩子稍后可以救回这只小鸡。
- 救援按单词隔离，每次只让该错词重新走一遍“听、写、选、默”四段新学路线；四段全对才原子地移出错题库并归还一只小鸡，未全对则两者都保留。
- 已输出 H-5A 准备、H-5B 答对、H-5C 重试、H-5D 被抓四张 1194×834 V2 候选图；当前均为 `pending-xiaopi`，尚未接入 `src/` 或真实数据库。

## 2026-07-17 · H-5 小皮批准与生产接入

- 小皮批准 H-5A 至 H-5D V2；四张候选均改为 `xiaopi-approved`，旧“看答案”V1 继续保持作废。
- 新增正式 `LessonDictationScreen`、纯函数判定模型与样式：支持标准键盘/系统手写文本输入、正确/重试、“想不起来”和小鸡被抓四态。
- “想不起来”只发出 `onForgot({ wordId })`，视觉组件不直接操作数据库；application 层负责错题/救援事务，阶段 H 全部完成前孩子可见导航继续门控。
- Playwright 已加入开发依赖并安装本机 Chromium 测试运行时，不进入生产 PWA 包；技能客户端实际点击跳过成功，文本状态与画面一致且无控制台错误。
- 新增模型测试后，全量自动测试 100/100、生产构建与四张 1194×834 生产回归均通过。下一项为 H-6 课程结束页视觉候选。

## 2026-07-17 · H-6 庆祝角色裁决与结束页候选

- 第一轮屁股超人三态被小皮否决；新裁决为永久废弃屁股超人，结束页改由母鸡妈妈竖大拇指说“真棒！”。产品说明、规格与资产待办已同步，防止旧方案回流。
- 小皮已批准新母鸡动作；绿幕母版、透明批准版与 public 副本分别落档，生产副本 SHA-256 为 `EBCFBB1898BEF2D31D5E0B2C8F2A2CFE1F35803DCD6DE0AE633B56CC6DF004C3`。
- 已输出 H-6 V1 1194×834 整页候选：母鸡气泡、今日战报（4 新词、6 复习、3 天连胜）、鸡蛋 +1 与“回农场”。当前为 `pending-xiaopi`，尚未接入正式完成事务。
- Playwright 初始态与回农场点击态均通过，文本状态和画面一致，控制台无错误。
- Claude 未提交的 `F4_STUDY_FLOW_PLAN.md` 仍有两处屁股超人旧注释；为避免覆盖共享工作，已在 F4-CHG-021 留下提交前替换提醒。

## 2026-07-17 · H-6 V2 小皮批准与生产接入

- 小皮对 H-6 提出两项最终修改：移除框架顶部贴纸，并让聊天气泡黑线连续无断层；V1 随之作废。
- H-6 V2 使用闭合 SVG 路径绘制气泡，顶部贴纸已完全移除；1194×834 候选图经小皮确认，状态改为 `xiaopi-approved`。
- 新增正式 `LessonFinishScreen`、结束战报归一化模型与样式；母鸡妈妈竖拇指说“真棒！”，显示新词、复习、连胜、鸡蛋 +1 和唯一的“回农场”操作。
- 正式组件仅发出回农场事件，不越层写数据库或自行结算课程；真实课程完成、发蛋与导航由阶段 H 整流集成统一接入。
- 1194×834 生产回归与 `lesson_finish_ready` / `lesson_finish_returning` 浏览器状态通过，控制台无错误；全量自动测试 102/102、生产构建通过。

## 2026-07-17 · 阶段 H 最终整流(Claude)

- H-1~H-6 已批准组件全部接通真实数据:`useLesson` 视觉桥 + `LessonFlowScreen` 容器按步骤类型分发;App 开放农场 ↔ 学习流生产路由,手写游戏/救援/家长页导航继续门控。
- 归约语义按阶段 H 定稿修订:答错**原地重试**(不做队尾重考);FSRS 每词每日首答定调(错→Again 不被重试的 Good 覆盖);默写/收尾用组件文本客观判定(F4-CHG-012),字迹不留档;选择/听音无"想不起来",默写"想不起来"→ H-5D 被抓 + 救援入队。
- H-6 结束页接真实战报:课程侧新词/复习 + 农场侧 DAY/连胜/蛋(+N);完课发蛋连胜事务在最后一步 dispatch 内原子完成,"回农场"只做导航。egg 词条已回填批准插图 `egg-f4-v2`,其余缺图词自动走 H-1B 无图版。
- 断点恢复:每步落库 `kv['lesson:{dayKey}']`,隔天自动清理;浏览器验证"描红中刷新 → 重进直达描红卡"。
- 回归:`tsc` 无错、104/104 自动测试、生产 PWA 构建(预缓存 21 项含母鸡竖拇指图);1194×834 浏览器全流程走查通过(起名 → 16 步 → 答错重试 → 想不起来被抓 → 结束页真数据 → 回农场,蛋 +1、待救 ×1,控制台无错误)。本会话浏览器面板截图管线超时,像素级截图对照与 A2759 真机验收(断网冷启动/PWA)留给 Codex/爸爸收尾。
- `F4_STUDY_FLOW_PLAN.md` 升级 v2:屁股超人残留注释已改为"母鸡妈妈竖拇指结束页(旧方案永久废弃)";新增 S-7 复习头部人设、S-8 救援屏接口两项待裁决。

## 2026-07-17 · 救援闭环数据层

- 爸爸裁决 S-8 直接复用现有听看、描红、选择、默写四张学习卡，不再等待小皮逐屏审核；本轮完成后直接进入首轮上线。
- 新增四阶段救援领域模型与应用用例：旧记录默认从听开始，队列按 `capturedAt` FIFO，选择/默写答错原地重试，阶段逐步落库可刷新恢复。
- 第四段正确后在同一 Dexie 事务中删除该救援记录并更新 `seen.lastSeenAt`；重复/迟到事件被阶段和队首双重门控，不会误操作下一词。
- 应用测试验证救援不改 FSRS、session、鸡蛋、连胜或农场小鸡；新增领域/应用测试 5/5 通过。
- 已开放 `farm → rescue` 生产路由并新增救援桥/容器，四段直接复用 H-1/H-2/H-3/H-5 生产组件；救援听看强制无图，默写隐藏“想不起来”。
- 四张学习卡仅增加可选头部配置，普通学习流默认文案不变；救援统一显示“救援 · 第 N 步 / 4”和待救数量。
- 默写正确只先保留组件正确态，必须点“继续”才原子完成救援；返回农场或刷新前不会提前删除错词。
- `FarmHomeViewModel` 已暴露 `newWordsPaused`；暂停日任务板显示真实“连续 N 天！”与“今天复习 N 个老朋友。”，保留原进度与开始按钮。
- 新增目标测试 7/7 通过，TypeScript 与生产 PWA 构建通过，预缓存仍为 21 项。

## 2026-07-17 · 救援闭环上线前收口

- 全量自动测试 110/110、`npm run build` 与生产 PWA 构建通过；Workbox 预缓存 21 项。
- 新增可复现生产 QA 脚本 `scripts/qa-rescue.mjs`，验证四阶段、选择/默写错答原地重试、答对后立即点继续的持久化竞态、第四段继续前记录保留、继续后原子删除并更新 seen、角标 1→0。
- 新鲜 Chromium 1194×834 截图覆盖暂停任务板、救援四阶段、农场角标前后与断网救援；最终归档图全部肉眼复查无布局问题，Stage H 基准未覆盖。
- 批量 Chromium 截图曾出现黑块合成伪影；QA 脚本增加 900ms 稳定绘制等待并停用截图动画，问题图使用独立进程逐张重抓后干净。该问题不复现在页面状态、交互或最终归档图中。
- Service Worker 断网冷启动可进入救援选择卡，页面所有图片完成解码；全流程控制台无应用错误。
- 有头浏览器额外发现 `/favicon.ico` 404，已用现有正式 `icon-192.png` 显式登记 favicon 并复测消除。
- 文档已登记 S-1 延期、S-3/S-8 爸爸已裁决并直接生产、S-7 保持现状；手写小游戏与家长页继续门控，词库抽查不阻塞本轮首发。
